import { AgentThreadWorkspace } from "@/components/canvas/agent-thread-workspace";

export function CreationAgentEntry() {
    return <div className="w-full"><AgentThreadWorkspace embedded canvasId="" nodeCount={0} references={[]} open onOpen={() => undefined} onCollapse={() => undefined} /></div>;
}
